{
  "name": "@avalon/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "echo 'Bun runs source directly; no build step needed for now'",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@avalon/game-core": "workspace:*",
    "@avalon/protocol": "workspace:*",
    "hono": "^4.6.16",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.7.3"
  }
}
